

/**
 * Returns the 5 characters whose heights are closest to the user's
 * @param {number} height - a user's height in cm
 * @returns {Array} - 5 characters whose heights are closest to the user's
 */
export function searchHeights(height, data) {
	let top5 = data.slice(0, 5);
	return top5;
}